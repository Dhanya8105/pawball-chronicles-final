fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
httpx==0.27.2
pillow==10.4.0
cloudinary==1.41.0
python-multipart==0.0.12
python-dotenv==1.0.1

# CV pipeline is Gemini Vision (app/services/vision_cv.py) — a single
# gemini-flash-lite-latest call with an image, no local model weights. Free key at
# https://aistudio.google.com/app/apikey. The previous YOLOv8 + CLIP stack
# and scripts/download_models.sh were removed when CV moved off local models.
google-genai>=1.0.0
