fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
httpx==0.27.2
pillow==10.4.0
cloudinary==1.41.0
python-multipart==0.0.12

# CV pipeline is now Claude Vision (app/services/vision_cv.py) — a single
# Anthropic Messages API call with an image, no local model weights. The
# previous YOLOv8 + CLIP stack (torch, torchvision, transformers,
# ultralytics, faiss-cpu, diffusers, huggingface_hub) and
# scripts/download_models.sh have been removed.
anthropic>=0.30.0
